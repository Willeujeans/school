def ga_tsp(initial_population, distances, generations):
    # initial_population: list of paths [tuples]
    # distances: a dictionary of distances
    # generations: number of generations the routine must run
    pass