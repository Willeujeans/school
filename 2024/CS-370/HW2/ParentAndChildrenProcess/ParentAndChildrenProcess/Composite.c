#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef char* String; // Strings are an array of characters, this is defined for readability
typedef char** StringArray; // An array of Strings is a double pointer, this is defined for readability

int composite(int n) {
    return 0;
}
